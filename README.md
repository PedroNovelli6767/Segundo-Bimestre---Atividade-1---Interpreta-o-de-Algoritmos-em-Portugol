1-Quando o código é executado aparece no console a frase “Olá Mundo”.


2-Quando o código é executado aparecera 2 frases no console, a primeira é “Olá Mundo”, e a segunda irá aparecer “Como vai”. As frases estarão na mesma linha e sem o espaço separando-as.


3- Quando o código é executado aparecera 2 frases no console, a primeira é “Olá Mundo”, e a segunda irá aparecer “Como vai”. As frases estarão em linhas diferentes, essa é a diferença.


4- Quando o código é executado ele pedira para você escrever o seu nome, fazendo isso aparecera no console seu nome.


5-a) Quando o código é executado aparecera 3 linhas com números no console, a primeira linha aparecera o numero 123, a segunda linha aparecera a soma de 2 números 100+200: aparecera no console o numero 300, e na terceira linha vai aparecer só os números 100+200:aparecera no console só a soma(100+200) sem o resultado.

b) a diferença é que no linha 6 aparecera no console o resultado da soma, e na linha 7 aparecera no console só a soma sem o resultado.


6-o código fala que todos tem 21 anos, ai ele compara o 21 anos e ver se ele é maior que 18, escrevendo no console a frase “maior de idade”.
